[pytest]
testpaths = tests
pythonpath = services/api
asyncio_mode = auto
markers =
    browser: needs a real Playwright browser (skipped automatically if unavailable)
filterwarnings =
    ignore::DeprecationWarning
